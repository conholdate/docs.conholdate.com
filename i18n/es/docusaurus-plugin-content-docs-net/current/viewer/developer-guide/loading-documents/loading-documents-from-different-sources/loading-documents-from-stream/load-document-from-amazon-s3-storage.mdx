---
title: "Cargar desde el almacenamiento de Amazon S3"
sidebar_label: "Cargar desde el almacenamiento de Amazon S3"
sidebar_key: "net-viewer-load-document-from-amazon-s3-storage"
sidebar_position: 5
description: "Este artículo explica cómo cargar un documento desde Amazon S3 Storage con GroupDocs.Viewer en sus aplicaciones .NET."
product: "viewer"
platform: "net"
total_version: "current"
included_product_version: "current"
version: "current"
source_repo: "viewer-net"
source_path: "net/developer-guide/loading-documents/loading-documents-from-different-sources/loading-documents-from-stream/load-document-from-amazon-s3-storage.md"
source_commit: "270bea1e2528043f99c68ff8a72edc20b3816a48"
source_sha256: "47500ce8f73f12bb69855f80703a09ac6722d2dc13dbc74f497c22137615033f"
locale: "es"
translated_from: "en"
translation_model: "qwen3-next"
translation_pipeline_version: "2026-09-02-mdx-protection-v3"
translation_source_sha256: "d6d721ebc363c09e5b130fc96e57a8c43a220e307875132d9b80716d8df2c57e"
translation_updated_at: "2026-09-15T11:47:38.291Z"
translation_status: "ai-draft"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

:::info
GroupDocs.Viewer for .NET se incluye en Conholdate.Total for .NET.
:::

El siguiente fragmento de código muestra cómo cargar un documento desde el almacenamiento de Amazon S3.

:::info
Ejecutar este código requiere instalar el paquete NuGet <a href="https://nuget.org/packages/AWSSDK.S3" target="_blank" rel="nofollow noopener noreferrer">AWSSDK.S3</a>.
:::

<Tabs groupId="example1">

<TabItem value="c" label="C#" default>

```csharp
using System.IO;
using System.Threading.Tasks;
using Amazon.S3;
using Amazon.S3.Model;
using GroupDocs.Viewer;
using GroupDocs.Viewer.Options;
// ...

string key = "sample.docx";
Stream stream = await DownloadFileAsync(key);

using (Viewer viewer = new Viewer(stream))
{
    HtmlViewOptions viewOptions = HtmlViewOptions.ForEmbeddedResources();
    viewer.View(viewOptions);
}

static async Task<Stream> DownloadFileAsync(string key)
{
    AmazonS3Client client = new AmazonS3Client();
    string bucketName = "my-bucket";
    GetObjectRequest request = new GetObjectRequest
    {
        Key = key,
        BucketName = bucketName
    };

    using (GetObjectResponse response = await client.GetObjectAsync(request))
    {
        MemoryStream stream = new MemoryStream();
        response.ResponseStream.CopyTo(stream);
        stream.Position = 0;
        return stream;
    }
}
```

</TabItem>

<TabItem value="vb-net" label="VB.NET">

```vb
Imports System.IO
Imports System.Threading.Tasks
Imports Amazon.S3
Imports Amazon.S3.Model
Imports GroupDocs.Viewer
Imports GroupDocs.Viewer.Options
' ...

Module Program
    Sub Main(args As String())
        Dim key As String = "sample.docx"
        Dim stream As Stream = DownloadFileAsync(key).GetAwaiter().GetResult()

        Using viewer As New Viewer(stream)
            Dim viewOptions As HtmlViewOptions = HtmlViewOptions.ForEmbeddedResources()
            viewer.View(viewOptions)
        End Using
    End Sub

    Private Async Function DownloadFileAsync(ByVal key As String) As Task(Of Stream)
        Dim client As New AmazonS3Client()
        Dim bucketName As String = "my-bucket"
        Dim request As New GetObjectRequest With {
                .Key = key,
                .BucketName = bucketName
                }

        Using response As GetObjectResponse = Await client.GetObjectAsync(request)
            Dim stream As New MemoryStream()
            response.ResponseStream.CopyTo(stream)
            stream.Position = 0
            Return stream
        End Using
    End Function
End Module

```

</TabItem>

</Tabs>
